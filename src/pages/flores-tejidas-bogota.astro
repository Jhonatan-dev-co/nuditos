---
import CitySeoLanding from '../components/CitySeoLanding.astro';
import { getLiveProducts } from '../lib/supabase';
import { CITY_LANDINGS } from '../data/seo-landings';

export const prerender = false;

const products = await getLiveProducts();
const config = CITY_LANDINGS['flores-tejidas-bogota'];
---

<CitySeoLanding config={config} products={products} />